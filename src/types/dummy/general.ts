import { IPagition } from "../pagination";

export namespace GeneralDummyData {
  export const PageMeta: IPagition = {
    page: 0,
    take: 0,
    itemCount: 0,
    pageCount: 0,
    hasPreviousPage: false,
    hasNextPage: false,
  };
}
